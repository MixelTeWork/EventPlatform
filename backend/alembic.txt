# init
python -m bafser alembic init

# add revision
python -m bafser alembic revision



## standalone usage

### add version
alembic revision --autogenerate -m "version name"

### upgrade
alembic upgrade head

### set version (for existing db without alembic)
alembic stamp %version_id%
